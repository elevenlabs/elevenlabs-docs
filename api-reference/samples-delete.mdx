---
title: "Delete Sample"
openapi: "DELETE /v1/voices/{voice_id}/samples/{sample_id}"
---

<RequestExample>

```Bash Curl
curl -X 'DELETE' \
  'https://api.elevenlabs.io/v1/voices/<voice-id>/samples/<sample-id>' \
  --header 'accept: application/json' \
  --header 'xi-api-key: <xi-api-key>'
```

```Python Python
import requests

url = "https://api.elevenlabs.io/v1/voices/<voice-id>/samples/<sample-id>"

headers = {
  "Accept": "application/json",
  "xi-api-key": "<xi-api-key>"
}

response = requests.delete(url, headers=headers)

print(response.text)
```

</RequestExample>