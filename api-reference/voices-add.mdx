---
title: "Add Voice"
openapi: "POST /v1/voices/add"
---

<RequestExample>

```Bash Curl
curl -X 'POST' \
  'https://api.elevenlabs.io/v1/voices/add' \
  --header 'accept: application/json' \
  --header 'xi-api-key: 450c3bff6ce6479735d997a40dca68ec' \
  --header 'Content-Type: multipart/form-data' \
  --form 'name=OliviaLexa' \
  --form 'files=@sample1.mp3;type=audio/mpeg' \
  --form 'files=@sample2.mp3;type=audio/mpeg' \
  --form 'description=sweet' \
  --form 'labels={"accent":"American"}'
```

```Python Python
import requests

url = "https://api.elevenlabs.io/v1/voices/add"

headers = {
  "Accept": "application/json",
  "xi-api-key": "450c3bff6ce6479735d997a40dca68ec"
}

data = {
    'name': 'OliviaLexa',
    'labels': '{"accent": "American"}',
    'description': 'sweet'
}

files = [
    ('files', ('sample1.mp3', open('sample1.mp3', 'rb'), 'audio/mpeg')),
    ('files', ('sample2.mp3', open('sample2.mp3', 'rb'), 'audio/mpeg'))
]

response = requests.post(url, headers=headers, data=data, files=files)
print(response.text)

```

</RequestExample>
