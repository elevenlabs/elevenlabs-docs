---
title: "Get Voice"
openapi: "GET /v1/voices/{voice_id}"
---

<RequestExample>

```Bash Curl
curl -X 'GET' \
  'https://api.elevenlabs.io/v1/voices/<voice-id>?with_settings=false' \
  --header 'accept: application/json' \
  --header 'xi-api-key: <xi-api-key>'
```

```Python Python
import requests

url = "https://api.elevenlabs.io/v1/voices/<voice-id>?with_settings=<true|false>"

headers = {
  "Accept": "application/json",
  "xi-api-key": "<xi-api-key>"
}

response = requests.get(url, headers=headers)

print(response.text)
```

</RequestExample>